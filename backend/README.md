# Backend README
- This is the README file for the backend of this project, written using Python (FastAPI).

## Getting the server to run
- Just run `fastapi dev` right now after activating your virtual environment in the current folder. The entrypoint has already been specified in `pyproject.toml`.
